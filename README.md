Sources for the plugin `plugin.adrally.adcolony`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.adrally.adcolony" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
